import { Router } from 'express';

// own


export default () => {
  const app = Router();
  // auth(app);
  // user(app);
  // account(app);
  // transaction(app);
  // tag(app);
  // rule(app);
  // recurrent(app);
  // budget(app);
  // attachment(app);
  return app
}