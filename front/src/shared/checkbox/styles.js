import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  checked: {
    background: '#f50057',
    width: '10px',
    height: '60px',
    marginLeft: '-20px'
  },
  unchecked: {
    width: '20px',
    height: '60px',
    marginLeft: '-20px'
  }
}));
