import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  root: {
  },
  indexColumn: {
    width: '1em',
    padding: 0,
  },
  deleteButton: {
    cursor: 'pointer',
  },
}));
