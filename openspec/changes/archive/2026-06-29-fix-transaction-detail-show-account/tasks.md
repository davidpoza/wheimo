## 1. Component

- [x] 1.1 Inject `AccountsService` into `TransactionDetailsDialogComponent`
- [x] 1.2 Add a `accountName` getter that resolves `tx.accountId` against `accountsService.accounts()`

## 2. Template

- [x] 2.1 Add an info-row for account name in the `info-section` of the details tab (above "From" or as the first row)

## 3. i18n

- [x] 3.1 Add `transactions.details.account` key to `frontend/public/i18n/es.json`
